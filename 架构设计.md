# 架构设计

## 1. 项目定位

本项目是一个基于 GitHub Pages 的**个人技术博客站点**，采用“纯静态托管 + Markdown 内容生产”的模式：

- 博客系统（`/blog`）：使用 Docsify 渲染 Markdown 内容，提供搜索、分页等能力；
- 在线写作入口（`/blog/admin/`）：基于浏览器 + GitHub API 的轻量写作后台，用于直接向仓库写入文章与图片。

运行期无后端服务，所有内容直接由 GitHub Pages 提供；在线写作由浏览器在用户侧调用 GitHub API 完成。

## 2. 架构目标

- **低运维成本**：无需服务器，仅依赖静态托管。
- **内容可持续生产**：新增文章与照片流程简单、可重复。
- **访问性能可控**：通过缩略图与静态资源直出提升加载速度。
- **可演进性**：后续可无缝扩展自动化发布、搜索、CDN 优化等能力。

## 3. 总体架构

```mermaid
flowchart LR
    author[作者] --> localMd[本地 Markdown\nblog/articles]
    author --> webEditor[在线写作页面\nblog/admin/index.html]

    webEditor --> githubAPI[GitHub API\n(contents 接口)]
    githubAPI --> repoFiles[仓库中文件\nblog/articles & blog/images]

    localMd --> docsify[Docsify 渲染\nblog/index.html]
    repoFiles --> docsify

    docsify --> githubPages[GitHub Pages 静态托管]
    home[index.html 站点首页] --> githubPages
```

## 4. 分层设计

### 4.1 展示层（Frontend）

- `index.html`：站点首页，采用 Apple 风格的 Hero + 最近文章卡片布局，突出“技术博客”定位；
- `blog/index.html`：Docsify 容器页，负责加载 Markdown、侧边栏、搜索与分页插件，并通过自定义 CSS 做了 Apple 风格的卡片化阅读体验；
- `blog/admin/index.html`：在线写作页面，提供配置 GitHub 仓库、录入元信息、编辑 Markdown、上传图片并一键发布的能力。

特征：

- 纯静态资源（HTML/CSS/JS），无自建后端；
- 内容阅读完全由 Docsify 在浏览器端渲染；
- 在线写作直接从浏览器调用 GitHub API 完成文件写入。

### 4.2 内容层（Content）

- 博客内容：`blog/README.md`、`blog/_sidebar.md`、`blog/articles/*.md`；
- 图片资源：`blog/images/*`，供 Markdown 正文通过相对路径 `![](../images/xxx)` 引用。

### 4.3 生产层（Content Produce）

内容生产有两条路径：

1. **本地写作路径**
   - 在本地编辑器中直接维护 `blog/articles/*.md`；
   - 按需在 `blog/images/` 下添加图片；
   - 更新 `blog/_sidebar.md` 与（可选）`blog/README.md` 中的“最近文章”区域；
   - 提交并推送到仓库，由 GitHub Pages 自动构建发布。

2. **在线写作路径**
   - 在浏览器访问 `/blog/admin/`；
   - 配置仓库信息与 GitHub Personal Access Token（仅保存在浏览器 `localStorage`）；
   - 在页面中输入标题、日期、标签、分类等信息，并撰写 Markdown 正文；
   - 选择本地图片文件，页面会将其上传到 `blog/images/` 目录；
   - 点击发布后，通过 GitHub Contents API 将文章写入 `blog/articles/*.md`，图片写入 `blog/images/*`。

两条路径最终都以 Markdown 与图片文件的形式落在同一个仓库中。

### 4.4 依赖层（Dependencies）

- 前端 CDN 依赖：Docsify、Prism、search/pagination/copy-code/zoom-image 等插件；
- 运行平台：GitHub Pages（静态托管）。

## 5. 核心流程

### 5.1 本地博客发布流程

1. 新增/修改 `blog/articles/*.md`；
2. 更新 `blog/_sidebar.md` 导航和（可选）`blog/README.md` 中的“最近文章”列表；
3. 在 `blog/images/` 下维护文章插图资源；
4. 提交并推送到仓库；
5. GitHub Pages 发布后，Docsify 在浏览器端按路由加载并渲染内容。

### 5.2 在线写作与发布流程

```mermaid
flowchart LR
    author[作者] --> webEditor[在线写作页面\n/blog/admin/]
    webEditor --> githubAPI[GitHub API\n(contents)]
    githubAPI --> repoFiles[blog/articles & blog/images]
    repoFiles --> docsify[Docsify 渲染\n/blog/]
    docsify --> githubPages[GitHub Pages]
```

流程说明：

1. 作者在浏览器中访问 `/blog/admin/`；
2. 在页面中填入 Token、仓库信息与文章内容；
3. 浏览器通过 GitHub Contents API 将 Markdown 与图片写入仓库；
4. Docsify 渲染仓库中的 Markdown 文件，对读者而言与本地写作无差别。

## 6. 目录与职责

- `index.html`：技术博客首页，提供主入口与最近文章预览；
- `blog/`：博客展示与内容文件：
  - `index.html`：Docsify 容器页（阅读界面）；
  - `README.md`：博客首页内容；
  - `_sidebar.md`：侧边栏与文章导航；
  - `articles/`：文章 Markdown 文件；
  - `images/`：文章中引用的图片资源；
  - `admin/index.html`：在线写作入口页面。

## 7. 非功能设计

- **性能**
  - 纯静态托管，Docsify 只拉取必要的 Markdown 文件；
  - 首页仅做轻量级“最近文章”列表渲染，避免复杂数据处理；
  - 在线写作页面仅在提交时访问 GitHub API，无额外长连接。
- **可用性**
  - 本地写作与在线写作两条路径互相独立、互不依赖；
  - 在线写作入口将 Token 保存在浏览器 `localStorage` 中，避免重复输入；
  - 移动端通过 CSS 媒体查询适配。
- **可维护性**
  - 内容与展示解耦：Markdown/图片与前端模板分离；
  - 内容存储完全依赖 GitHub 仓库，版本历史清晰；
  - 在线写作逻辑集中在 `blog/admin/index.html`，可独立演进。

## 8. 风险与改进建议

- **GitHub API 限流风险**：在线写作完全依赖 GitHub API，频繁提交可能触发限流。
  - 建议：控制单次发布的图片数量，必要时增加简单的失败重试与提示。
- **Token 管理风险**：PAT 一旦泄露会带来仓库安全风险。
  - 建议：使用细粒度 Token，并限定在单一仓库；定期轮换 Token。
- **前端 CDN 依赖稳定性风险**：Docsify 与插件依赖公共 CDN。
  - 建议：关键脚本可在后续迁移为本地托管或增加备用 CDN。

## 9. 结论

当前架构专注于个人技术博客场景：简单、稳定、低成本。通过“静态页面 + Docsify + GitHub API”实现了接近动态站点的写作与发布体验，同时保留了 GitHub Pages 的部署优势和纯静态的运维成本。
